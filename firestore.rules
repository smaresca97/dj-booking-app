rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return signedIn() && request.auth.uid == userId;
    }

    function isBootstrapAdminEmail() {
      return signedIn() && request.auth.token.email in ['smaresca97@gmail.com'];
    }

    function isAdmin() {
      return signedIn()
        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == 'approved';
    }

    function isApprovedDjOrAdmin() {
      return signedIn()
        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == 'approved'
        && (
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'dj'
          || get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
        );
    }

    function isApprovedVenueOrAdmin() {
      return signedIn()
        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == 'approved'
        && (
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'venue'
          || get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
        );
    }

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();

      allow create: if isOwner(userId)
        && (
          (
            request.resource.data.role == 'dj'
            && request.resource.data.status == 'pending'
          )
          || (
            request.resource.data.role == 'venue'
            && request.resource.data.status == 'pending'
          )
          || (
            isBootstrapAdminEmail()
            && request.resource.data.role == 'admin'
            && request.resource.data.status == 'approved'
          )
        );

      allow update: if isAdmin()
        || (
          isOwner(userId)
          && resource.data.role != 'admin'
          && resource.data.status != 'approved'
          && request.resource.data.status == 'pending'
          && request.resource.data.role in ['dj', 'venue']
          && request.resource.data.diff(resource.data).affectedKeys().hasOnly([
            'email',
            'name',
            'photoUrl',
            'lastLoginAt',
            'role',
            'status',
            'requestedAt'
          ])
        )
        || (
          isOwner(userId)
          && request.resource.data.role == resource.data.role
          && request.resource.data.status == resource.data.status
        );

      allow delete: if false;
    }

    match /djProfiles/{djId} {
      allow read: if true;
      allow create, update: if isApprovedDjOrAdmin() && (request.auth.uid == djId || isAdmin());
      allow delete: if false;
    }

    match /availability/{availabilityId} {
      allow read: if true;
      allow create, update: if isApprovedDjOrAdmin()
        && request.resource.data.djId is string
        && (request.resource.data.djId == request.auth.uid || isAdmin())
        && request.resource.data.date is string
        && request.resource.data.status in ['AVAILABLE', 'NOT_AVAILABLE'];
      allow delete: if false;
    }

    match /venueAssignments/{assignmentId} {
      allow read: if isAdmin()
        || (
          isApprovedVenueOrAdmin()
          && resource.data.venueUserId == request.auth.uid
        );
      allow create, update: if isAdmin()
        || (
          isApprovedDjOrAdmin()
          && request.resource.data.djType == 'registered'
          && request.resource.data.djId == request.auth.uid
          && request.resource.data.venueUserId is string
        );
      allow delete: if isAdmin();
    }

    match /bookingRequests/{requestId} {
      allow create: if request.resource.data.djId is string
        && request.resource.data.djName is string
        && request.resource.data.clientName is string
        && request.resource.data.clientPhone is string
        && request.resource.data.date is string
        && request.resource.data.from is string
        && request.resource.data.to is string
        && request.resource.data.type is string
        && request.resource.data.place is string
        && request.resource.data.status == 'pending';

      allow read: if isAdmin()
        || (
          isApprovedDjOrAdmin()
          && resource.data.djId == request.auth.uid
        );

      allow update: if isAdmin()
        || (
          isApprovedDjOrAdmin()
          && resource.data.djId == request.auth.uid
          && request.resource.data.djId == resource.data.djId
          && request.resource.data.status in ['pending', 'accepted', 'rejected', 'cancelled']
        );

      allow delete: if false;
    }
  }
}
